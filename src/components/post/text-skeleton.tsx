// import { Skeleton } from '@mui/material'

export const TextSkeleton = ({ width = 500, count = 7 }) => {
  // const skeletons = Array.from({ length: count }, (_, index) => (
  //   <Skeleton key={index} variant="text" animation="wave" width={width} />
  // ))

  return <>Loading</>
}
